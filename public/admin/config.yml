local_backend: true

backend:
  name: git-gateway
  branch: main

media_folder: public/images
public_folder: /images

collections:
  - name: pages
    label: Pages
    files:
      - name: homepage
        label: Homepage
        file: src/data/homepage.json
        fields:
          - { label: Title, name: title, widget: string }
          - { label: Subtitle, name: subtitle, widget: richtext }
          - { label: About, name: about, widget: markdown }
          - label: Tech Icons
            name: icons
            widget: list
            field: { label: Tech Name, name: icon, widget: string, hint: "e.g. csharp, azure, kubernetes" }
      - name: footer
        label: Footer
        file: src/data/footer.json
        fields:
          - { label: Owner Name, name: owner_name, widget: string }
          - { label: Copyright Suffix, name: copyright_suffix, widget: string, required: false }
          - { label: Tagline, name: tagline, widget: string, required: false }
          - label: Social Links
            name: social_links
            widget: list
            fields:
              - { label: Label, name: label, widget: string }
              - { label: URL, name: url, widget: string, hint: "e.g. https://github.com/username" }
              - { label: Icon Class, name: icon, widget: string, required: false, hint: "Bootstrap Icon class, e.g. bi-github" }
      - name: devicons
        label: Dev Icons
        file: src/data/devicons.json
        fields:
          - label: SVG Icon Overrides
            name: icons
            widget: list
            summary: "{{fields.key}}"
            fields:
              - label: Tech Name
                name: key
                widget: select
                options:
                  - csharp
                  - svelte
                  - microsoftsqlserver
                  - kubernetes
                  - azure
                  - netlify
              - label: SVG Markup
                name: svg
                widget: text
                hint: "Paste raw SVG markup. Leave blank to keep using the CDN devicon class."
                required: false
      - name: empty_states
        label: Empty States
        file: src/data/empty-states.json
        fields:
          - { label: Homepage Latest Posts Empty Message, name: home_latest_posts_empty, widget: string }
          - { label: Homepage Featured Work Empty Message, name: home_featured_work_empty, widget: string }
          - { label: Blog Index Empty Message, name: blog_index_empty, widget: string }
          - { label: Portfolio Index Empty Message, name: portfolio_index_empty, widget: string }
      - name: llms
        label: LLMs.txt
        file: src/data/llms.json
        fields:
          - label: Content
            name: body
            widget: text
            hint: "Full llms.txt content served at /llms.txt. Follow the llmstxt.org spec: start with '# Name', then '> tagline', then sections."
      - name: contact
        label: Contact
        file: src/data/contact.json
        fields:
          - { label: Page Title, name: title, widget: string }
          - { label: Page Subtitle, name: subtitle, widget: string }
          - { label: Intro Copy, name: intro, widget: text }
          - { label: Meta Description, name: meta_description, widget: text }
          - { label: Name Field Label, name: name_label, widget: string }
          - { label: Email Field Label, name: email_label, widget: string }
          - { label: Subject Field Label, name: subject_label, widget: string }
          - { label: Message Field Label, name: message_label, widget: string }
          - { label: Honeypot Label, name: honeypot_label, widget: string }
          - { label: Submit Button Label, name: submit_label, widget: string }
          - { label: Thanks Page Title, name: thanks_title, widget: string }
          - { label: Thanks Page Message, name: thanks_message, widget: text }
          - { label: Thanks Page Link Label, name: thanks_link_label, widget: string }

  - name: blog
    label: Blog
    folder: src/content/blog
    create: true
    slug: "{{slug}}"
    extension: md
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, date_format: YYYY-MM-DD, time_format: false }
      - { label: Description, name: description, widget: string }
      - label: Categories
        name: categories
        widget: list
        required: false
        hint: "Maximum 3 categories"
      - { label: Body, name: body, widget: richtext }

  - name: portfolio
    label: Portfolio
    folder: src/content/portfolio
    create: true
    slug: "{{slug}}"
    extension: md
    fields:
      - { label: Title, name: title, widget: string }
      - { label: Date, name: date, widget: datetime, date_format: YYYY-MM-DD, time_format: false, required: false }
      - { label: Description, name: description, widget: string }
      - { label: Is Featured, name: is_featured, widget: boolean, required: false, default: false }
      - label: Tags
        name: tags
        widget: list
        required: false
        hint: "Maximum 3 tags"
      - { label: Body, name: body, widget: richtext }
