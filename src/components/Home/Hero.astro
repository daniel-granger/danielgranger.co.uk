---
import '../../assets/css/hero.css';
import { marked } from 'marked';
import homepage from '../../data/homepage.json';
import { deviconSvgMap, type DevIconKey } from '../../data/devicons';

const subtitleHtml = marked.parseInline(homepage.subtitle);
const needsDeviconStyles = homepage.icons.some((icon) => !deviconSvgMap[icon as DevIconKey]);
---

{
	needsDeviconStyles && (
		<>
			<link rel="preload" as="style" href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css" onload="this.onload=null;this.rel='stylesheet'" />
			<noscript><link rel="stylesheet" href="https://cdn.jsdelivr.net/gh/devicons/devicon@latest/devicon.min.css" /></noscript>
		</>
	)
}

<section class="hero">
	<div class="row">
		<div class="col-12 title">
			<h1 id="hero-title" class="display-3"><b>{homepage.title}</b></h1>
			<div class="render-code hero-tag" set:html={subtitleHtml}></div>

			<div class="dev-icon-container mt-3">
				{homepage.icons.map((icon) => {
					const svgMarkup = deviconSvgMap[icon as DevIconKey];
					return svgMarkup ? (
						<span class="dev-icon-svg" data-dev-icon aria-hidden="true" set:html={svgMarkup}></span>
					) : (
						<i class={`devicon-${icon}-plain`} data-dev-icon aria-hidden="true"></i>
					);
				})}
			</div>
		</div>
	</div>
</section>

<script>
	document.querySelectorAll('.dev-icon-container > [data-dev-icon]').forEach((icon) => {
		icon.addEventListener('mouseenter', () => icon.classList.add('colored'));
		icon.addEventListener('mouseleave', () => icon.classList.remove('colored'));
	});
</script>
